import { LoginForm } from "@/components/account/auth-forms";
export default function LoginPage() { return <div className="auth-page"><div><h1>Ingresá a tu cuenta</h1><p>Usamos una sesión segura del navegador. No guardamos tokens de acceso en almacenamiento local.</p></div><LoginForm /></div>; }
