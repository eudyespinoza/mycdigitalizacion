import { AddressManager } from "@/components/account/address-manager";
export default function AddressesPage() { return <><div className="catalog-title"><h1>Direcciones de entrega</h1><p>Buscá por código postal, completá la dirección y confirmá el punto o sus coordenadas.</p></div><AddressManager /></>; }
