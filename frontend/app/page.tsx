import Image from "next/image";
import Link from "next/link";
import { ArrowRight, CreditCard, MapPin, ShieldCheck, Truck } from "@phosphor-icons/react/dist/ssr";
import { Hero } from "@/components/home/hero";
import { PromotionCarousel } from "@/components/home/promotion-carousel";
import { ScheduledPromotionPopup } from "@/components/home/promotion-popup";
import { SiteFooter } from "@/components/layout/site-footer";
import { SiteHeader } from "@/components/layout/site-header";
import { ProductCard } from "@/components/product/product-card";
import { normalizeMediaUrl, serverGet } from "@/lib/api";
import type { CatalogResponse, Category, Product, StorefrontHome } from "@/lib/types";

export const dynamic = "force-dynamic";
const emptyHome: StorefrontHome = { settings: { public_name: "mycdigitalizacion", announcement: "", contact_email: "" }, hero_slides: [], promotion_slides: [], collections: [], promotion_popups: [] };

async function loadHome() {
  const [homeResult, categoryResult, productResult] = await Promise.allSettled([serverGet<StorefrontHome>("/storefront/home/"), serverGet<Category[]>("/categories/"), serverGet<CatalogResponse>("/products/?ordering=relevance")]);
  return { home: homeResult.status === "fulfilled" ? homeResult.value : emptyHome, categories: categoryResult.status === "fulfilled" ? categoryResult.value : [], products: productResult.status === "fulfilled" ? productResult.value.results : [], cmsError: homeResult.status === "rejected", categoryError: categoryResult.status === "rejected", catalogError: productResult.status === "rejected" };
}

export default async function HomePage() {
  const { home, categories, products, cmsError, categoryError, catalogError } = await loadHome();
  return <><SiteHeader categories={categories} />{home.settings.announcement && <div className="announcement">{home.settings.announcement}</div>}<main id="contenido">{cmsError && <section className="inline-error cms-error shell" role="alert"><strong>No pudimos cargar las novedades de la tienda.</strong><p>El catálogo sigue disponible. Podés reintentar sin perder tu recorrido.</p><a href="/" className="button secondary">Reintentar novedades</a></section>}<Hero slide={home.hero_slides[0]} />{categoryError ? <section className="inline-notice shell"><p>Las categorías no están disponibles en este momento.</p></section> : <section className="category-rail shell" aria-labelledby="categories-title"><h2 className="sr-only" id="categories-title">Categorías</h2>{categories.slice(0, 6).map((category, index) => <Link key={category.id} href={`/catalogo?category=${category.slug}`}><span>{index + 1}</span>{category.name}</Link>)}<Link href="/catalogo"><span>+</span>Ver todas</Link></section>}<section className="offer-band shell"><div><strong>Encontrá lo que necesitás</strong><p>Buscá por nombre o recorré el catálogo por categoría.</p></div><Link href="/catalogo">Ver catálogo <ArrowRight size={20} /></Link></section>{catalogError ? <section className="empty-state home-empty shell"><h2>No pudimos cargar los productos</h2><p>La tienda no respondió. Reintentá para ver precios y disponibilidad actuales.</p><a className="button primary" href="/">Reintentar productos</a></section> : products.length > 0 ? <section className="featured shell"><div className="section-heading"><h2>Productos para descubrir</h2><Link href="/catalogo">Ver más productos <ArrowRight size={18} /></Link></div><div className={`product-grid home-grid count-${Math.min(products.length, 4)}`}>{products.slice(0, 4).map((product, index) => <ProductCard product={product} priority={index < 2} key={product.id} />)}</div></section> : <section className="empty-state home-empty shell"><h2>El catálogo todavía está vacío</h2><p>Cuando el equipo publique productos, aparecerán acá automáticamente.</p><Link className="button secondary" href="/catalogo">Revisar catálogo</Link></section>}<PromotionCarousel slides={home.promotion_slides} />{home.collections.map((collection) => { const source = normalizeMediaUrl(collection.desktop_image_url); const selected = collection.product_ids.map((id) => products.find((product) => product.id === id)).filter((product): product is Product => Boolean(product)); return <section className="cms-collection shell" key={collection.id}>{source && <div className="collection-image"><Image src={source} alt={collection.alt_text} fill sizes="(max-width: 768px) 100vw, 52vw" style={{ objectPosition: `${collection.focal_x}% ${collection.focal_y}%` }} /></div>}<div className="collection-copy"><h2>{collection.title}</h2>{collection.body && <p>{collection.body}</p>}{collection.cta_label && <Link className="button primary" href={collection.cta_url}>{collection.cta_label}</Link>}</div>{selected.length > 0 && <div className={`product-grid collection-products count-${Math.min(selected.length, 3)}`}>{selected.map((product) => <ProductCard key={product.id} product={product} />)}</div>}</section>; })}<section className="service-notes shell" aria-label="Cómo funciona la compra"><article><ShieldCheck size={30} /><h2>Datos protegidos</h2><p>La identidad y los datos fiscales se procesan en pasos separados y auditables.</p></article><article><Truck size={30} /><h2>Envío cotizado</h2><p>El costo se consulta al proveedor antes de confirmar el pedido.</p></article><article><CreditCard size={30} /><h2>Pago confirmado</h2><p>Mercado Pago redirige, pero solo el estado del servidor confirma el resultado.</p></article><article><MapPin size={30} /><h2>Dirección precisa</h2><p>Podés confirmar el punto en mapa o por texto y coordenadas.</p></article></section></main>{home.promotion_popups[0] && <ScheduledPromotionPopup campaignId={home.promotion_popups[0].id} title={home.promotion_popups[0].title} body={home.promotion_popups[0].body ?? ""} ctaLabel={home.promotion_popups[0].cta_label} ctaUrl={home.promotion_popups[0].cta_url} />}<SiteFooter contactEmail={home.settings.contact_email} /></>;
}
