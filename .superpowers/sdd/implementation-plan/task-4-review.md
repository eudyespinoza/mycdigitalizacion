# Task 4 independent review

## Verdict

- **SPEC COMPLIANCE: FAIL.** The storefront compiles and the mocked happy path runs, but a newly registered customer cannot complete checkout against the real backend. Four release blockers remain: post-login CSRF writes fail, customers cannot supply the DNI required by checkout, shipping addresses cannot reach a confirmed state, and uploaded CMS/catalog media is not deliverable in production.
- **CODE QUALITY: FAIL.** The type/lint/build baseline is clean and several trust boundaries are handled correctly, but the test suite replaces the critical backend semantics with permissive mocks and therefore reports green while the real end-to-end flow is broken.
- **Finding count:** 13 REQUIRED, 4 OPTIONAL.
- **Severity:** 4 P0, 9 P1, 3 P2, 1 P3.

## Scope and evidence

Reviewed `e1e95cd..068309a7f36008eba70afbfd63f2bd65bc17283d` against:

- `.superpowers/sdd/implementation-plan/task-4-brief.md`
- `PRODUCT.md`
- `docs/visual-direction.md`
- `.impeccable/mocks/decision/pulso-comercial-approved.png`
- `.impeccable/review/desktop.png`, `.impeccable/review/mobile.png`, and `.impeccable/review/hero-repro.png`
- the Django URL configuration, DRF serializers/views, checkout/address services, production Compose/Caddy routing, and OpenAPI contract tests
- the complete `design-taste-frontend`, `ui-ux-pro-max`, `emil-design-eng`, and `impeccable` skill instructions

Per the task constraint, the Impeccable detector was **not** run and `DESIGN.md` was **not** created.

## Audit health score

| Dimension | Score | Key finding |
| --- | ---: | --- |
| Accessibility | 1/4 | Primary/focus colors fail contrast; drawers/sheets and the map alternative are not keyboard complete. |
| Performance | 2/4 | Local hero is optimized, but real product/promo media is unoptimized and currently undeliverable. |
| Responsive design | 2/4 | Layout collapses exist, but the approved hierarchy regresses and 360/768/1024 are not exercised. |
| Theming | 4/4 | The approved token palette, radii, and tinted shadows are centralized and consistent. |
| Implementation integrity | 1/4 | Critical mocks diverge from CSRF, identity, media, address, and checkout behavior. |
| **Total** | **10/20** | **Acceptable only as an incomplete prototype; not releasable.** |

## REQUIRED findings

| # | Severity | Before | After | Why / evidence / reproduction |
| ---: | --- | --- | --- | --- |
| 1 | **P0** | `frontend/lib/api.ts:43-64` caches one CSRF token for the lifetime of the client module. `backend/api_views.py:322-348` calls Django `login()`, which rotates the CSRF token. | Clear/refetch CSRF after login/logout and retry one CSRF failure with a newly fetched token; add a real cookie-aware integration test. | **Repro:** register/verify/login without a hard reload, navigate client-side to fiscal data or addresses, then submit a POST. The header still sends the pre-login token while the cookie contains the rotated token, so Django returns 403. The mock API never validates cookies or `X-CSRFToken`, hiding the defect. |
| 2 | **P0** | Registration only accepts email/password/consent (`frontend/components/account/auth-forms.tsx:15-18`), the account explicitly says profile editing is unavailable (`frontend/components/account/account-dashboard.tsx:55-72`), and `/customers/me/` is GET-only (`backend/api_views.py:361-367`). Checkout rejects a customer with no DNI at `backend/commerce/checkout.py:163-170`. | Publish a protected profile/identity write contract and UI for the required customer fields, including DNI, then verify the persisted masked response before checkout. | **Repro:** register, verify email, log in, create billing/address data, and confirm checkout. A normal customer has no route that calls `CustomerProfile.set_dni`, so `/checkout/` returns `identity_missing`. Only staff admin can set DNI today. This breaks the product's core end-to-end purpose. |
| 3 | **P0** | Geocoding always sets `needs_review=True` (`backend/locations/services.py:64-92`). The UI only calls reverse geocoding after a move over 150 m and otherwise renders non-interactive text (`frontend/components/account/address-manager.tsx:17-20`). A far reverse lookup also keeps `needs_review=True` (`backend/locations/services.py:96-119`). Checkout rejects it at `backend/commerce/checkout.py:104-113`. | Add an explicit confirmation transition for both unchanged/near pins and the second confirmation after a far reverse lookup; expose an operable text/coordinate confirmation path and persist `reviewed_at`. | **Repro:** save and geocode an address, leave the suggested pin unchanged, and continue. There is no confirmation button, so the address remains blocked. Move it >150 m and press “Confirmar nuevo punto”; the returned address still requires review, but the UI now removes the button. Shipping checkout can never accept either path. |
| 4 | **P0** | Server reads use `http://backend:8000/api/v1` (`frontend/lib/api.ts:3-40`); CMS serializers turn media into absolute request-host URLs (`backend/landing/serializers.py:18-32`), product media also emits image URLs (`backend/catalog/serializers.py:13-16`), and production Caddy proxies `/api`, `/healthz`, and `/static` but not `/media` (`infra/caddy/Caddyfile:4-20`). `next.config.ts:3-10` has no remote image policy. | Serve `/media/*` through the public reverse proxy and make API image URLs public/same-origin, or normalize them in the frontend; configure Next image optimization for the final host. | **Repro:** run the production topology with an uploaded hero/product image. Server-rendered CMS URLs resolve to `backend:8000`, which a browser cannot resolve and Next Image does not allow; a public `/media/...` request is routed to the frontend and 404s. The Playwright mock uses relative `/campaigns/...` assets, so all four E2E tests pass incorrectly. |
| 5 | **P1** | The “Cuenta e identidad” step only GETs addresses and billing profiles (`frontend/components/checkout/checkout-flow.tsx:39-44`), never `/customers/me/`, `/identity/status/`, or `/identity/validate/`. The review step contains no cart items/totals/address summary or back controls, and every quote/checkout exception is rendered as `provider_down` (`:45-61`). | Implement the required sequence and recovery states using API error codes: account/profile/email, identity status/consent, address or pickup, shipping, full authoritative review, confirmation, and redirect. Preserve back navigation and entered state. | **Repro:** open checkout with an empty cart, missing DNI, invalid billing profile, unconfirmed address, or expired quote. All are local/domain problems, but the UI claims “El proveedor no respondió.” Users cannot correct the actual cause in place. |
| 6 | **P1** | Catalog UI exposes category, client-side name/first-variant-price sorting, and client slicing only (`frontend/components/catalog/catalog-browser.tsx:10-24`). It prints a note instead of price, availability, offer, brand, attribute facets, removable chips, category tree, or server pagination. The backend product contract contains none of those fields (`backend/catalog/serializers.py:19-48`) and the views only support category/search (`backend/api_views.py:107-146`). | Complete the versioned catalog contract and drive all required URL filters/facets/pagination from it; do not substitute explanatory copy for specified functionality. | **Repro:** visit `/catalogo` and try to filter by price, availability, offer, brand, or variant attribute. No control or shareable URL state exists. A five-level category tree is flattened into one select. This is a direct Task 4 completeness failure even though the UI is honest about the missing API. |
| 7 | **P1** | `frontend/app/page.tsx:11-25` catches any CMS/category/product failure and replaces it with empty arrays, so an outage is shown as “El catálogo se está preparando.” `home.collections` is never rendered; a hard-coded collection is rendered instead. `frontend/components/home/hero.tsx:28-37` ignores `mobile_image_url` and all safe-height fields, and the promo carousel ignores its mobile image/focal/safe-height contract. | Keep CMS empty, loading, and error states distinct; render scheduled collections/product IDs; honor authored mobile images, focal points, and safe heights with responsive `<picture>`/Image behavior. | **Repro:** stop the API and load `/`: no error or retry appears. Publish a landing collection or mobile-specific hero in admin: the storefront ignores it. A failure in categories also discards an otherwise valid CMS hero because all reads share one `Promise.all`. |
| 8 | **P1** | Cart line totals are recalculated in the browser (`frontend/components/cart/cart-page.tsx:13`) despite the “no business-total calculation” rule. Quantity changes fire on every input event and `CartProvider.perform` has no sequencing/cancellation (`frontend/components/cart/cart-provider.tsx:27-46`), so older responses can overwrite newer ones. PDP add failures are thrown from a `void` handler with no local error state (`frontend/components/product/product-purchase.tsx:11-21`). | Render only authoritative line/totals fields, serialize or cancel mutations, disable relevant controls while pending, and surface price/stock/change errors with retry/recovery. Extend the cart API if a line total/change notice is required. | **Repro:** rapidly change a quantity 1→2→3 under network throttling; responses may settle out of order and leave the cart at an older quantity. Reject an add-to-cart request; the PDP gives no error message. No stock/price-change notices exist because neither API nor UI publishes them. |
| 9 | **P1** | Pending payment maps to a generic `checking` panel but is fetched only once (`frontend/components/orders/order-result.tsx:8-11` and `checkout-flow.tsx:9-13`). Order detail has no fulfillment timeline/tracking events and pickup copy is a placeholder (`frontend/components/orders/order-detail.tsx:6-19`); the order serializer exposes no timeline/tracking data (`backend/commerce/serializers.py:70-105`). | Add bounded polling/manual retry for pending results, an order-specific recovery link, and an authoritative fulfillment/pickup/shipping timeline contract with only API-permitted resume/retry actions. | **Repro:** return from Mercado Pago while payment is pending: the page stays “consultando” forever unless reloaded. Open a shipped/pickup order: no carrier timeline, tracking, or configured pickup information is available. Redirect query distrust itself is correct. |
| 10 | **P1** | White text on `--magenta #F34887` is only **3.45:1**, and cyan text/focus on white is **2.64:1** (`frontend/app/styles.css:4-19,39,44-49,107,178`). | Use approved darker interaction/text tokens or ink text while preserving the Pulso palette; ensure primary labels reach 4.5:1 and focus/UI boundaries reach 3:1. | **Repro:** compute WCAG relative luminance for `#fff/#F34887` and `#08AECD/#fff`. Primary CTAs are normal-size text and fail WCAG 2.2 AA; the global cyan focus ring also misses non-text contrast. |
| 11 | **P1** | Cart drawer and mobile filters visually behave as modal surfaces but have no initial focus, Escape handling, focus containment, inert background, or focus return (`frontend/components/cart/cart-drawer.tsx:8-44`, `frontend/components/catalog/catalog-browser.tsx:24`, `frontend/app/styles.css:189-191,342-344`). The Leaflet marker is drag-only and displayed coordinates are not an operable alternative (`address-map-inner.tsx:7-10`, `address-manager.tsx:20`). | Implement complete dialog/sheet focus behavior and a keyboard/text coordinate/address confirmation control; associate form errors with fields and focus the first invalid field. | **Repro:** open cart or filters with keyboard, press Tab repeatedly, then Escape. Focus can move behind the surface and Escape does nothing. A keyboard-only user cannot adjust/confirm the map point. This violates the explicit keyboard-first and non-map-path requirements. |
| 12 | **P1** | The captured implementation materially misses the binding approved comp: the 720×617 stacked logo is forced into a 250×76/180×64 box and becomes a tiny wordmark (`site-header.tsx:18-20`, `styles.css:57-60,299-301`); the desktop hero wraps to three lines and mobile to four oversized lines (`styles.css:75-85,308-317`); a one-product payload leaves an unfinished four-column merchandising field (`styles.css:95-110`). | Recompose the header around the supplied asset (or use an approved horizontal treatment), restore the comp's two-line desktop hierarchy and calmer mobile scale, and adapt the featured layout for 1-3 products without stretching or empty-card-wall whitespace. | **Repro:** compare `.impeccable/review/hero-repro.png` and `desktop.png` to `pulso-comercial-approved.png`, then inspect `mobile.png`. These are not subjective micro-polish issues: brand recognition, first-viewport hierarchy, and merchandising density all visibly regress. The huge mobile collection headline reinforces the scale problem at `styles.css:331-333`. |
| 13 | **P1** | Vitest mostly tests pure helpers/presence and Playwright runs only 1440 plus Pixel 7 (`frontend/playwright.config.ts:19-22`). The mock ignores CSRF/cookies/login rotation, DNI/identity, address confirmation, real media URLs, stock changes, CMS failures, facets, result polling, and fulfillment timeline (`frontend/tests/mock-api.mjs:1-35`). | Add contract-faithful red/green tests for every Task 4 boundary and explicit 360/768/1024/1440 projects. Assert request method/path/body/CSRF/cookies, post-login writes, real media URL behavior, full checkout state order, keyboard focus, and error recovery. | **Repro:** current lint/type/Vitest/build/Playwright all pass while REQUIRED 1-4 make the real flow unusable. The “mocked checkout” skips login, identity APIs, address confirmation, and cart contents, returning `pending_review` directly from a permissive mock. |

## OPTIONAL findings

| # | Severity | Before | After | Why / evidence |
| ---: | --- | --- | --- | --- |
| 14 | **P2** | Product/PDP/promo API images use `unoptimized` (`product-card.tsx:9`, `app/producto/[slug]/page.tsx:34-44`, `promotion-carousel.tsx:12`), bypassing responsive encoding after media delivery is repaired. | Configure allowed public media origins or a same-origin loader and let Next emit sized AVIF/WebP variants. | Real catalogs can upload large originals; sending them unchanged harms LCP/data usage. The authored PNGs do contain embedded Impeccable prompt provenance, which is a positive finding. |
| 15 | **P2** | Reduced motion globally forces every animation/transition to 1 ms (`frontend/app/styles.css:385-387`). | Remove spatial/decorative motion while retaining short opacity/color/state feedback intentionally per component. | The current kill switch meets the basic “less motion” intent but removes useful state-change feedback rather than designing a reduced-motion alternative. |
| 16 | **P2** | Popup dismissal is stored only in `sessionStorage` for one tab (`frontend/components/home/promotion-popup.tsx:13-17`), and the test checks only a callback (`storefront-ux.test.tsx:84-99`). | Define a documented campaign frequency policy (session/day/until campaign changes), persist it accordingly, and test remount/new-tab/time behavior. | The current behavior is defensible as “once per tab session,” but it is not a configurable frequency system and is not actually covered end to end. |
| 17 | **P3** | The entire header is a client component (`frontend/components/layout/site-header.tsx:1-49`) even though logo/search/category navigation are static. | Keep the server-rendered shell and isolate menu/cart count/drawer into small client leaves if bundle profiling shows value. | This is an architecture/payload refinement, not a correctness blocker; the current Server/Client boundary is valid React. |

## Positive findings

- The root layout preserves the exact direction contract and seed `db399cd4` in production-surviving markup.
- The two authored campaign PNGs carry embedded `impeccable:prompt` provenance and have useful, non-invented alt text.
- Rubik/Nunito Sans, Phosphor, the approved color world, pill/card/input radius rules, tinted shadows, 44 px base controls, and explicit mobile collapses are implemented consistently.
- Public data reads are Server Components and interactive commerce/map/form behavior is isolated in client components; no unsupported reviews, stars, wishlists, WhatsApp, fake urgency, or payment approval from redirect query parameters was found.
- API calls use same-origin `/api/v1`, `credentials: include`, stable error normalization, and server-authoritative cart/order totals at the summary boundary.
- Checkout correctly treats a 202/no-checkout-URL response as pending identity review and does not reserve/payment-approve in the UI.
- No blanket `transition: all`, scroll listener, gradient blob, glass panel, or arbitrary motion dependency use was found.

## Verification performed

| Command | Result |
| --- | --- |
| `pnpm --dir frontend lint` | PASS |
| `pnpm --dir frontend typecheck` | PASS |
| `pnpm --dir frontend test:ci` | PASS, 2 files / 10 tests |
| `pnpm --dir frontend build` | PASS, 15 application routes reported |
| `pnpm --dir frontend test:e2e` | PASS, 4 tests (desktop/mobile) |
| `APP_ENV=test python -m pytest tests/test_openapi_semantics.py tests/test_api_contracts.py tests/test_checkout_api.py -q` from `backend/` | PASS, 12 tests |

The green command results are real, but they do not override the verified contract mismatches above.

## Recommended repair order

1. Fix REQUIRED 1-4 and add failing integration tests before changing presentation.
2. Complete the backend/frontend identity, address confirmation, catalog, cart-notice, media, and order timeline contracts.
3. Rebuild checkout as an ordered, recoverable flow with domain-specific errors and an authoritative review.
4. Repair WCAG contrast and modal/map keyboard behavior.
5. Recompose the header/hero/single-product merchandising against the approved comp at 360/768/1024/1440.
6. Re-run all verification plus one bounded desktop/mobile visual pass. Do not use the Impeccable detector until the task that explicitly authorizes it.

## Fix Round 1 verdict

Reviewed backend/infra commit `c0f858fb231372523dbae410edb2794fbe91859a` and frontend commit `04cc4bed0fe8ffc1e857a271bad7e7ae1ca9707d`. The frontend hash supplied for this round (`04cc4be0b78c9eef30a5689e3e18ff134e7ed452`) does not exist in this repository; `04cc4be` resolves to the latter commit. This was a read-only implementation review: the four required design skills were applied, the Impeccable detector was not run, and no `DESIGN.md` was created.

- **Status count:** 3 RESOLVED, 10 PARTIAL, 0 wholly UNRESOLVED.
- **SPEC COMPLIANCE:** **FAIL — changes required.** F1, F3, F4, F5, F7, F9, F10, F11, F12, and F13 retain required behavior or craft gaps.
- **CODE QUALITY:** **FAIL — changes required.** Static analysis, builds, SQLite/PostgreSQL tests, and current E2E tests pass, but production-only media delivery and real-contract error handling remain broken while permissive mocks report green.

| Finding | Status | Before | After | Why / exact evidence / reproduction |
| ---: | --- | --- | --- | --- |
| F1 | **PARTIAL** | A module-lifetime CSRF token survived Django login rotation. | `frontend/lib/api.ts:56-94` clears the cache after successful login/logout and performs at most one named-CSRF retry. Post-login writes therefore fetch a new token. | The retry cannot recognize the actual backend failure. Django has no `CSRF_FAILURE_VIEW`; its middleware returns HTML. Repro from `backend/`: an `enforce_csrf_checks=True` login POST without a token returns `403 text/html`, so `readBody()` becomes `null` and `isNamedCsrfFailure()` at `frontend/lib/api.ts:67-71` is false. `frontend/tests/fix-round-1.test.tsx:53-78` invents a JSON `{code:"csrf_failed"}` response that the application does not publish. Logout cache clearing is present, but the required safe real-contract retry is not. |
| F2 | **RESOLVED** | A normal registrant could not write checkout profile fields or DNI. | Registration accepts/persists name and phone (`backend/accounts/serializers.py:32-44`, `backend/api_views.py:374-400`); authenticated `PATCH /customers/me/` writes profile/DNI and returns only the masked DNI (`backend/api_views.py:480-515`); the account form exposes it (`frontend/components/account/profile-form.tsx:9-48`). | The cookie-aware journey at `backend/tests/test_task4_storefront_contracts.py:12-147` and PostgreSQL-focused run both pass. Invalid DNI is rejected without overwriting the encrypted value (`:237-253`). The normal customer path now reaches a safe pending-identity checkout without staff intervention. |
| F3 | **PARTIAL** | Neither the unchanged/near nor far-pin flow could become checkout-eligible. | The backend confirmation action persists `reviewed_at` (`backend/api_views.py:748-786`, `backend/locations/services.py:120-155`), and the UI now supplies unchanged/near confirmation, draggable pin, reverse lookup, and text coordinate controls (`frontend/components/account/address-manager.tsx:54-80`). | The far flow renders both “Usar dirección escrita” and “Usar dirección encontrada” (`address-manager.tsx:80`), but reverse lookup has already overwritten the stored point/source as `manual` (`backend/locations/services.py:93-117`), so the backend necessarily rejects `written` as `address_choice_mismatch`; its own test asserts that 409 (`backend/tests/test_task4_storefront_contracts.py:349-384`). Also, `MapContainer center` is initialization-only and `address-map-inner.tsx:7-10` has no `useMap().setView`, key, or recenter lifecycle, so selecting/editing another point moves the marker without reliably recentering the viewport. Near and reverse-choice confirmation work; the advertised written choice and Leaflet lifecycle do not. |
| F4 | **PARTIAL** | Browser-visible media used internal hosts and production did not serve `/media`. | Serializers now emit relative media paths, Caddy serves `/media/*`, backend/Caddy share `media_data`, and frontend components use optimized Next `<Image>` (`backend/catalog/serializers.py:22-31`, `backend/landing/serializers.py:26-37`, `infra/caddy/Caddyfile:19-23`, `compose.prod.yaml:58-59,163-167`). | Production optimization is still broken. `next.config.ts:7-12` only rewrites `/media` when `API_PROXY_TARGET` exists, while the production frontend defines only `NODE_ENV` (`compose.prod.yaml:145-154`). Live repro with `API_INTERNAL_URL=http://127.0.0.1:4010/api/v1`, no `API_PROXY_TARGET`, and a production Next server: both `/_next/image?url=%2Fmedia%2Fcms%2Fhero.png&w=640&q=75` and the product equivalent returned **400** “The requested resource isn't a valid image”; the Next log reported a null upstream. Playwright masks this by setting `API_PROXY_TARGET` (`frontend/playwright.config.ts:10-17`). Public Caddy `/media` and volume tests pass, but the actual optimized browser URL does not. |
| F5 | **PARTIAL** | Checkout skipped identity/account APIs, lacked an authoritative review/back path, and collapsed errors into provider failure. | `frontend/components/checkout/checkout-flow.tsx:34-125` now implements account/identity, delivery, shipping, and review with cart/address/fiscal summaries and back/edit controls. | Recovery keys do not match real backend codes. Frontend expects `email_unverified`, `address_unconfirmed`, `billing_profile_required`, and `provider_down` (`frontend/lib/checkout-recovery.ts:4-18`); backend emits `email_not_verified`, `address_review_required`, `billing_profile_invalid`, `shipping_quote_changed`, and provider `not_configured`/`unavailable`/`timeout`/`invalid_response` (`backend/commerce/checkout.py:104-122,140-203,300-362`, `backend/api_views.py:967-990`). Exact failures therefore fall to generic step 3 instead of their recoverable step, and the unit test checks the invented names (`frontend/tests/fix-round-1.test.tsx:132-138`). Pickup is also always offered (`checkout-flow.tsx:122`) and accepted (`backend/commerce/checkout.py:104-106`) without consulting `SiteSettings.pickup_enabled`. The happy/pending-review path is complete; required error recovery and configured pickup gating are not. |
| F6 | **RESOLVED** | Catalog sorting/slicing were client-only and required facets/chips/tree/pagination were absent. | The versioned backend envelope now filters category descendants, brand, price, stock, offer, and typed attributes, computes recursive facets, orders, and paginates (`backend/catalog/storefront.py:11-275`, `backend/catalog/serializers.py:174-260`). The frontend round-trips URL state, renders facet controls, tree depth, removable chips, and server pagination (`frontend/app/catalogo/page.tsx:13-22`, `frontend/components/catalog/catalog-browser.tsx:10-40`). | Focused contract tests exercise descendants and typed filters (`backend/tests/test_task4_storefront_contracts.py:459-590`); PostgreSQL-focused tests pass. No client business sorting/pagination remains. |
| F7 | **PARTIAL** | CMS/category/catalog failures were conflated; collections and authored responsive fields were ignored. | `frontend/app/page.tsx:16-23` uses independent settled results, distinct recovery states, scheduled collections, product IDs, focal points, and CMS images. Hero/promo support mobile sources. | Responsive authoring is incomplete: hero defines tablet height but CSS never consumes `--hero-tablet-height` (`frontend/components/home/hero.tsx:9-11`, `frontend/app/styles.css:82,344-353`); promotions force `safe_height_mobile` at every viewport (`frontend/components/home/promotion-carousel.tsx:10-13`); collections ignore `mobile_image_url` and every safe-height field and hard-code `min-height:520px` (`frontend/app/page.tsx:23`, `frontend/app/styles.css:133-135`). Collection product IDs are resolved only against the first `/products/` page, silently dropping authored IDs outside it. Playwright also logged the collection image as LCP and requested `loading="eager"`. Partial-error separation is fixed, but the published responsive CMS contract is not honored end to end. |
| F8 | **RESOLVED** | The client recalculated totals, mutations could race, PDP add failures escaped, and no change notices existed. | The cart response publishes authoritative line/totals, availability, and notices (`backend/commerce/serializers.py:38-151`); `CartProvider` serializes mutations and applies responses in order (`frontend/components/cart/cart-provider.tsx:11-20`, `frontend/lib/mutation-queue.ts:1-10`); page/drawer render server values and notices, and PDP handles errors locally (`frontend/components/cart/cart-page.tsx:9-16`, `frontend/components/product/product-purchase.tsx:8-14`). | Backend notice tests and queue tests pass. Controls are disabled while pending and the UI never derives a business total. |
| F9 | **PARTIAL** | Result status fetched once; order detail lacked timeline/tracking/configured pickup. | Payment polling is bounded to six attempts with manual retry (`frontend/components/orders/order-result.tsx:8-30`), query parameters are distrusted, and backend order detail publishes bounded audit timeline, shipment, and pickup settings (`backend/commerce/serializers.py:175-294`). | Action/status vocabulary is wrong in the UI. Backend payment values are `not_started`, `pending`, `paid`, `failed`, `refunded`, `needs_attention` (`backend/commerce/models.py:245-251`), but `order-detail.tsx:9,17` labels/tests `approved`/`rejected` and only offers resume for `pending`/`rejected`; a real `failed` order is shown as raw “failed” with no permitted retry. The view ignores `pickup_information.enabled`, while F5 allows disabled pickup. Polling/timeline/tracking are resolved; authoritative resume and configured pickup behavior are not. |
| F10 | **PARTIAL** | Primary white/magenta text was 3.45:1 and cyan focus was 2.64:1. | Dark action tokens now give `#bd1d59`/white **6.03:1** and `#007f96`/white **4.69:1**, so primary CTA text and focus boundaries pass (`frontend/app/styles.css:9-11,41,48-50`). | The old `#f34887` token remains behind the small white cart-count text (`frontend/app/styles.css:8,75`), still **3.45:1** for 0.72rem text. Playwright only calculates the two new tokens (`frontend/tests/e2e/storefront.spec.ts:153-164`), so its green result does not cover the remaining failure. |
| F11 | **PARTIAL** | Cart/filter modal behavior and keyboard coordinates were absent. | Cart and mobile filters now set initial focus, trap Tab, close on Escape, inert the background, and return focus (`frontend/components/cart/cart-drawer.tsx:9-14`, `frontend/components/catalog/catalog-browser.tsx:10-18`); coordinate text inputs provide the non-map path (`address-manager.tsx:80`). | Leaflet still lacks a prop-change/recenter lifecycle as described in F3. Profile validation always focuses the first-name ref when any local error exists (`frontend/components/account/profile-form.tsx:14,20-29`), so a DNI-only error focuses a valid unrelated field rather than the first invalid field. Dialog behavior itself passed E2E; map lifecycle and complete validation-focus behavior remain. |
| F12 | **PARTIAL** | Logo was tiny, hero wrapped excessively, mobile type was oversized, and one item occupied a four-column field. | Refreshed `360.png`, `768.png`, `1024.png`, `1440.png`, and `hero-repro.png` show a legible horizontal logo crop, two-line desktop hero, calmer three-line mobile hero, and much closer first-viewport hierarchy to `pulso-comercial-approved.png`. | Sparse merchandising remains visibly unfinished at 768/1024/1440: `.product-grid.count-1` is a 280–360px column pinned to the far left (`frontend/app/styles.css:107-110,361-362`), leaving most of the authored shell blank. The approved comp uses the width compositionally rather than ending the section after one narrow card. The logo/hero/mobile fixes are accepted; adaptive sparse merchandising is not. |
| F13 | **PARTIAL** | Tests used permissive mocks and omitted required viewports/contracts. | Frontend now has 19 Vitest tests and Playwright projects at exact 360/768/1024/1440; the shared landing/PDP/cart path runs in all four. Backend adds real cookie/CSRF/profile/address/catalog/cart/order contracts and PostgreSQL coverage. | Browser output is **12 passed / 24 skipped** because most scenarios are hard-gated to one viewport (`frontend/tests/e2e/storefront.spec.ts:35-164`). More importantly, the mock publishes JSON CSRF failure and frontend-friendly checkout/status vocabularies that the real API does not, and Playwright injects the media rewrite absent in production (`frontend/playwright.config.ts:10-17`). Consequently F1, F4, F5, F9, and F10 reproduce against real contracts while the suite remains green. Viewport existence is resolved; contract fidelity is not. |

No additional standalone REQUIRED finding was opened: the production image failure belongs to F4, disabled pickup/status drift to F5/F9, and the LCP warning to F7/image performance.

### Fix Round 1 verification

| Command / check | Result |
| --- | --- |
| `pnpm lint`, `pnpm typecheck`, `pnpm test -- --run`, `pnpm build` from `frontend/` | PASS; 3 files / 19 tests; Next production build completed. |
| `pnpm exec playwright test` from `frontend/` | PASS as configured; **12 passed, 24 skipped** across 360/768/1024/1440; collection LCP warning observed. |
| `APP_ENV=test python -m pytest -q` from `backend/` | PASS; **162 passed, 13 skipped**. |
| `docker compose run --rm -e APP_ENV=test -e USE_POSTGRES_TEST_DB=true backend pytest -q tests/test_postgres_checkout.py tests/test_task4_storefront_contracts.py` | PASS; **21 passed** against PostgreSQL. |
| Ruff, Django `check`, migration drift check, and OpenAPI generation/validation | PASS. |
| `tests/test_media_topology.py` | PASS; 2 tests. |
| Development and production Compose `config --quiet` (production with review-only required values) | PASS. |
| Approved comp plus refreshed 360/768/1024/1440/hero screenshots | Inspected manually under the four skill craft floors; logo/hero/mobile accepted, sparse one-product wide layout rejected. |
| Production-mode Next optimizer with relative CMS/product `/media` URLs and no `API_PROXY_TARGET` | **FAIL; HTTP 400** for both optimized image requests. |

Green commands do not override the reproduced production and real-contract failures above.

## Fix Round 2 verdict

Reviewed backend/infra commit `d265ff02ac3fb8141cff972600839814aa116cbe` and frontend commit `5fca33918c366a423cb3a3dea6da8fbadfac56b4` against the ten Fix Round 1 partials, the actual Django/DRF/OpenAPI contracts, the production media path, the approved Pulso Comercial composition, and the refreshed `360.png`, `768.png`, `1024.png`, `1440.png`, and `hero-repro.png`. The four required design skills were read and applied again. Per the task constraint, the Impeccable detector was **not** run and `DESIGN.md` was **not** created.

- **Status count:** 9 RESOLVED, 1 PARTIAL, 0 UNRESOLVED.
- **SPEC COMPLIANCE:** **FAIL — one required change remains.** F7 still violates authored responsive focal/media behavior and unnecessarily preloads both hero assets.
- **CODE QUALITY:** **FAIL — one required change remains.** Runtime contracts, accessibility, build, and suites are otherwise healthy, but the responsive-image implementation and its test assert DOM intent instead of effective CSS/network behavior.

| Finding | Status | Before | After | Why / exact evidence / reproduction |
| ---: | --- | --- | --- | --- |
| F1 | **RESOLVED** | Django returned an HTML CSRF 403, so the client's named one-retry path could never run. | API middleware failures now return stable JSON through `CSRF_FAILURE_VIEW` (`backend/config/settings.py:137`, `backend/config/views.py:9-23`). The client recognizes only `403` + `csrf_failed`, clears the token, and recursively retries once; login/logout clear the rotated cache (`frontend/lib/api.ts:56-92`). | The real `Client(enforce_csrf_checks=True)` test covers missing and post-login stale tokens and succeeds with a freshly fetched token (`backend/tests/test_task4_fix_round2_contracts.py:20-57`). Frontend tests also prove one retry and no retry for unrelated/legacy 403s (`frontend/tests/fix-round-1.test.tsx:56-90`). |
| F3 | **RESOLVED** | Far reverse lookup made the advertised written choice invalid, and Leaflet did not follow coordinate changes. | Manual/reverse confirmation accepts either explicit `written` or `reverse` and persists the chosen result (`backend/locations/services.py:93-123,126-172`); `MapRecenter` calls `setView` whenever the point changes (`frontend/components/account/address-map-inner.tsx:7-15`). The text coordinates, >150 m lookup, two explicit choices, and focused confirmation status are operable (`frontend/components/account/address-manager.tsx:56-82`). | Backend tests cover both far written preservation and reverse normalization (`backend/tests/test_task4_fix_round2_contracts.py:60-131`); the map lifecycle unit test passes (`frontend/tests/map-viewport.test.tsx:1-17`), and Playwright completes unchanged/near plus far reverse paths in mobile and desktop modes (`frontend/tests/e2e/storefront.spec.ts:67-94`). |
| F4 | **RESOLVED** | `/media` was public through Caddy but Next production lacked the rewrite required by its optimizer. | The frontend image now bakes `API_PROXY_TARGET=http://backend:8000` during the production build, retains it at runtime, and Compose passes both build arguments and runtime variables (`frontend/Dockerfile:12-17`, `compose.prod.yaml:145-158`). The same-origin `/media` rewrite remains in `frontend/next.config.ts:7-12`; Caddy/backend share `media_data`. | `pnpm test:e2e:production` performed a real Next production build/server run and `GET /_next/image?url=%2Fmedia%2Fcms%2Fhero.png&w=640&q=75` returned **200**, an image content type, and a nontrivial body (`frontend/tests/production/media-optimizer.spec.ts:3-8`). This reproduces and closes the exact prior HTTP 400. |
| F5 | **RESOLVED** | Recovery used invented frontend codes and pickup ignored CMS configuration. | Recovery now enumerates the backend's exact checkout/resume/provider codes and returns the user to the affected step (`frontend/lib/checkout-recovery.ts:4-30`). Checkout loads authoritative settings, offers pickup only when enabled and operationally described, preserves review/back controls, and submits the selected fulfillment (`frontend/components/checkout/checkout-flow.tsx:36-130`). Backend rechecks pickup before and inside the transaction and on resume (`backend/commerce/checkout.py:102-141,148-193,221-245,342-413`). | OpenAPI enumerates the real CSRF/checkout/resume/provider code sets and focused backend tests cover disabled pickup and resume races (`backend/api_views.py:270-381,1296-1412`, `backend/tests/test_task4_fix_round2_contracts.py:134-350`). Vitest checks recovery for the exact names (`frontend/tests/fix-round-1.test.tsx:144-174`); Playwright verifies both full review and disabled-pickup UI (`frontend/tests/e2e/storefront.spec.ts:118-147`). |
| F7 | **PARTIAL** | Authored tablet/mobile heights, collection mobile media, focal points, and products outside page one were not honored. | Safe heights now become mobile/tablet/desktop CSS variables (`frontend/lib/campaign-presentation.ts:4-9`) and all campaign surfaces consume them at the relevant breakpoints (`frontend/app/styles.css:82,132,139,319-320,350-382`). Collections use `CampaignImage`, and `resolveCollectionProducts` walks subsequent server pages until every authored ID is found (`frontend/app/page.tsx:17-42,57-60`, `frontend/lib/campaign-presentation.ts:12-22`). | **Required gap remains:** mobile hero CSS hard-overrides the authored focal point with `.hero-media img { object-position: 66% center !important; }` (`frontend/app/styles.css:357-361`), defeating the inline `focal_x/focal_y` from `CampaignImage` (`frontend/components/home/campaign-image.tsx:5-9`). The component also renders separate desktop and mobile `<Image priority>` elements for the hero. Repro against the running build: requesting `/` emitted simultaneous image preloads for both `/media/cms/hero.png` and `/media/cms/hero-mobile.png`, each with a full `srcSet`; at <=768 px computed CSS uses 66%/center rather than the authored 58%/50%. This downloads/preloads the losing breakpoint asset and prevents CMS focal control. `frontend/tests/round-2-behavior.test.tsx:20-31` checks only the inline style and class, so it misses the effective `!important` override and network duplication. Use one responsive source selection (for example a real `<picture>`/loader strategy) and preserve the authored focal point at every breakpoint. |
| F9 | **RESOLVED** | UI vocabulary did not match backend statuses, failed payment had no resume action, and pickup configuration was ignored. | Order detail maps exactly `not_started/pending/paid/failed/refunded/needs_attention`, offers resume for the API-supported nonterminal/failed states, renders server timeline/shipment, and honors `pickup_information.enabled` (`frontend/components/orders/order-detail.tsx:9-18`). Backend serializer publishes configured pickup with a coherent default and bounded timeline/tracking (`backend/commerce/serializers.py:175-294`); checkout rejects pickup when explicitly disabled. | Vitest asserts a real `failed` order exposes “Retomar pago” and polling transitions `not_started -> pending -> paid` (`frontend/tests/fix-round-1.test.tsx:184-207`). Playwright proves bounded manual polling and server timeline/tracking (`frontend/tests/e2e/storefront.spec.ts:149-162`). Redirect query parameters remain distrusted. |
| F10 | **RESOLVED** | The small white cart badge remained on 3.45:1 magenta. | The badge now uses `--magenta-action: #bd1d59` with white (`frontend/app/styles.css:8-11,75`), matching the passing CTA token instead of the decorative magenta. | Playwright measures the computed badge background/text ratio at runtime and requires >=4.5, alongside primary text and focus contrast (`frontend/tests/e2e/storefront.spec.ts:164-180`); it passes in all four viewport projects. |
| F11 | **RESOLVED** | Leaflet did not recenter and DNI-only validation focused a valid first-name field. | `MapRecenter` handles prop changes (`frontend/components/account/address-map-inner.tsx:7-15`), profile validation focuses the first actual `[aria-invalid=true]` field (`frontend/components/account/profile-form.tsx:19-47`), and cart/filter surfaces retain initial focus, Escape, trap, inert background, and focus return (`frontend/components/cart/cart-drawer.tsx:9-14`, `frontend/components/catalog/catalog-browser.tsx:10-18`). | Unit tests cover recenter and DNI focus (`frontend/tests/map-viewport.test.tsx:7-16`, `frontend/tests/fix-round-1.test.tsx:174-182`). Playwright verifies address status focus, cart return focus, and mobile filter dialog focus/Escape (`frontend/tests/e2e/storefront.spec.ts:28-34,67-80,96-109`). |
| F12 | **RESOLVED** | One product remained a narrow card pinned left in a mostly empty desktop/tablet shell. | One-item home and collection grids now become a composed editorial split: large product media plus centered copy on wider screens, returning to a conventional single-column card at <=768 px (`frontend/app/styles.css:107-115,367-373`). | All refreshed captures show a legible horizontal logo, two-line desktop hero, calmer three-line 360 hero, and a one-product module that uses the available 768/1024/1440 width without fabricating inventory. The E2E landing asserts >520 px sparse-card width outside 360 (`frontend/tests/e2e/storefront.spec.ts:10-20`). Against the approved comp, remaining crop/content differences are within the established Pulso direction rather than a hierarchy/density failure. |
| F13 | **RESOLVED** | Mocks contradicted real CSRF/media/status contracts, and most viewport cases were skipped. | The mock now uses the real CSRF body, post-login token rotation, actual checkout/order status values, disabled-pickup behavior, `/media` URLs, responsive CMS payloads, and exact request assertions (`frontend/tests/mock-api.mjs:12-57`). Playwright defines exact 360/768/1024/1440 projects, runs the core landing/PDP/cart/checkout/contrast paths across all four, and limits skips to interaction-mode-specific map/filter/polling repetitions (`frontend/playwright.config.ts:19-24`, `frontend/tests/e2e/storefront.spec.ts:10-180`). A separate production-build test exercises the optimizer. | Final browser result is **34 passed, 6 intentionally skipped**, replacing Round 1's 12/24 split. Real backend middleware/OpenAPI tests independently cover boundaries a Node mock cannot prove. No permissive mock vocabulary was found hiding F1/F4/F5/F9/F10. The remaining F7 test blind spot is recorded on F7, not a general contract-fidelity failure. |

No new standalone REQUIRED regression was opened. The only release-blocking evidence is the responsive CMS image/focal defect retained under F7.

### Fix Round 2 OPTIONAL findings

| # | Severity | Before | After | Why / evidence |
| ---: | --- | --- | --- | --- |
| O1 | **P2** | The production Playwright harness starts `pnpm start` while `output: "standalone"` is configured. | Start `.next/standalone/server.js` in the production test, or execute the built frontend container. | Next printed `"next start" does not work with "output: standalone"`; the optimizer test still passed and the deployed Docker command is already correct (`frontend/Dockerfile:19-27`), so this is test-topology precision rather than a runtime blocker. |
| O2 | **P2** | The refreshed 768 capture uses an unusually tall single-product image/card, and the repeated featured/collection product creates a long page. | Consider a tablet-specific cap or alternate editorial crop once the catalog has representative inventory. | The sparse state is now compositionally intentional and usable, so this is polish, not a reason to invent products or reject F12. |

### Fix Round 2 verification

| Command / check | Result |
| --- | --- |
| `pnpm lint`, `pnpm typecheck`, `pnpm test:ci` from `frontend/` | PASS; **5 files / 25 tests**. |
| `pnpm test:e2e:production` from `frontend/` | PASS; **1/1**, real production Next build/server optimizer request to same-origin `/media`. |
| `pnpm test:e2e` from `frontend/` | PASS; **34 passed, 6 intentional interaction-mode skips** across exact 360/768/1024/1440 projects. |
| `APP_ENV=test pytest -q tests/test_task4_fix_round2_contracts.py tests/test_task4_storefront_contracts.py tests/test_catalog_cms_round1.py` from `backend/` | PASS; **38/38**. |
| `APP_ENV=test pytest -q` from `backend/` | PASS; **170 passed, 13 skipped**. |
| Approved comp plus refreshed `360.png`, `768.png`, `1024.png`, `1440.png`, `hero-repro.png` | Inspected manually under the four skill craft floors; logo/hero/sparse merchandising accepted. |
| Running-home HTML preload and computed-style inspection | FAIL for F7 only: both hero assets are preloaded and mobile focal is overridden to 66%/center. |

Green checks do not override the reproduced F7 network/CSS defect.

## Fix Round 3 verdict

Reviewed frontend commit `99427c8e643a99d3cf53ace21c25c3f1cd58cfa5` only against the sole remaining REQUIRED F7 from review commit `918679192a3f3a03ca34cff47faba05c2e1c0b08`. Concurrent Task5A work was not reviewed or modified. The four required design skill floors were applied again; the Impeccable detector was **not** run and `DESIGN.md` was **not** created.

- **Status count:** 1 RESOLVED, 0 PARTIAL, 0 UNRESOLVED.
- **SPEC COMPLIANCE:** **PASS.** No Task 4 REQUIRED finding remains.
- **CODE QUALITY:** **PASS.** F7 now has effective-CSS, selected-source, standalone-HTML, optimizer, and four-viewport regression evidence rather than DOM-intent coverage alone.

| Finding | Status | Before | After | Why / exact evidence / reproduction |
| ---: | --- | --- | --- | --- |
| F7 | **RESOLVED** | Mobile CSS replaced the CMS focal point with `66% center !important`, while two priority Next images caused desktop and mobile hero candidates to be emitted/preloaded together. | The override and breakpoint display rules are removed (`frontend/app/styles.css:86-89,355-358`). `CampaignImage` now renders one accessible Next `<Image>` inside a positioned `<picture>`, derives the optimized mobile `srcSet` with `getImageProps`, preserves authored `focal_x/focal_y`, and assigns `fetchPriority="high"` only to the one selected image (`frontend/components/home/campaign-image.tsx:1-13`). | Real standalone browser probe: at **360**, `currentSrc` was `/_next/image?url=%2Fmedia%2Fcms%2Fhero-mobile.png&w=384&q=75`; at **1024**, it was the desktop `hero.png&w=640`. Both returned **200 image/png**. At both sizes the hero contained exactly one `img` and one `source`, exposed `fetchPriority=high`, and computed `object-position: 58% 50%`. The standalone HTML test independently found one hero image, one mobile source, and zero duplicate hero preloads (`frontend/tests/production/media-optimizer.spec.ts:10-22`). The original same-origin optimizer request also remains 200 (`:3-8`). |

No new REQUIRED regression was found. The refreshed `360.png`, `768.png`, `1024.png`, `1440.png`, and `hero-repro.png` retain the accepted logo, hero hierarchy, crop, mobile scale, and sparse merchandising composition; the authored focal now controls every breakpoint.

### Fix Round 3 OPTIONAL finding

| # | Severity | Before | After | Why / evidence |
| ---: | --- | --- | --- | --- |
| O3 | **P3** | The production harness invoked unsupported `next start` for a standalone build. | It now starts the correct generated entrypoint, `node .next/standalone/frontend/server.js` (`frontend/playwright.production.config.ts:9-21`), and the warning is gone. | This is a material improvement and closes prior O1. If this harness later performs full visual/browser production tests, copy `.next/static` and `public` beside the standalone artifact as the Dockerfile already does (`frontend/Dockerfile:19-27`); the current request/HTML/media tests do not require those local assets. This is test-topology hardening, not a Task 4 blocker. |

### Fix Round 3 verification

| Command / check | Result |
| --- | --- |
| `pnpm lint`, `pnpm typecheck`, `pnpm test:ci` from `frontend/` | PASS; **5 files / 25 tests**. |
| `pnpm test:e2e:production` from `frontend/` | PASS; **2/2** using the actual standalone server entrypoint. |
| Standalone Chromium probe at 360 and 1024 | PASS; mobile/desktop optimized `currentSrc` selected correctly, each HTTP 200; one image, one source, high fetch priority, authored 58%/50% focal. |
| `pnpm exec playwright test tests/e2e/storefront.spec.ts:10` | PASS; **4/4** across exact 360/768/1024/1440 projects. |
| Refreshed five-image visual review | PASS under `design-taste-frontend`, `ui-ux-pro-max`, `emil-design-eng`, and `impeccable` craft floors. |

The collection LCP advisory appeared only after the intentionally full-page screenshot scrolled the sparse fixture; it is unrelated to the above-fold hero selection and does not reopen F7.
